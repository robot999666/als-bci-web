services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      CORS_ORIGINS: '["http://localhost:3000","http://127.0.0.1:3000"]'
    restart: unless-stopped
